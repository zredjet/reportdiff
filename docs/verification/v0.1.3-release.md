# v0.1.3 赤青オーバーレイ・領域設定の配布準備

確認日：2026-09-22。v0.1.2 以降の T3-6、T3-4a、T3-4b、T3-1a、独立した周期ゴールデン 5 件、大グループ内の並列化を含む。比較式・既定の許容値・依存バージョンは維持する。

## README と同梱サンプル

README に実際の HTML レポートから撮影した確認用オーバーレイを追加した。A のみ赤、B のみ青、共通は黒／グレー。判定用の輪郭・番号・除外色を含めず、補正前の入力を確認する。自作の架空帳票で `DRAFT COPY` の削除、`CHECKED` の追加、数値 80→85 を示す。

- サンプルの比較設定は normal・300dpi のまま、`report.raw_overlay: true` だけを有効化。3 箇所の判定・分類を維持する。
- 入力 A/B とレポートを 2 回生成し、17 ファイルのバイト一致を確認。
- 実 CLI と、生成日時・入力パスを除く JSON と全 13 PNG が一致。CLI・JSON の版は 0.1.3。
- README の 3 画面を v0.1.3 の HTML から再撮影し、目視確認。画像の描き足し・差分の加工は行わない。
- Chrome 153.0.8010.52、1280px / 390px で判定・確認用の切り替え、キーボード、PNG 単体、JavaScript なし、横幅、JSON 埋め込み、相違件数の不変を確認。ページエラー・外部リクエストは 0。
- 入力・生成コード・画面はプロジェクトの自作物として MIT License で提供。外部帳票・ロゴ・個人情報を使わず、既存依存の第三者通知を維持する。

[再生成手順](../samples/readme/README.md)と[検証記録](v0.1.3-sample.json)を参照。

## ローカル検証と配布

macOS Apple Silicon、.NET SDK 10.0.401 / Runtime 10.0.12。Release ビルドは警告・エラー 0、全 **1,330 テスト成功**、失敗・スキップ 0。元の PageMap 導入については [95 ケースの JSON・HTML・PNG 一致](t3-1a-pagemap.md)を確認済み。

win-x64 の自己完結・単一 exe を publish し、バンドル検査に成功。依存・.NET 10.0.12・必須ネイティブ DLL・PdfPig 管理 DLL・許諾文のハッシュを既存の承認値と照合し、FFmpeg・他 OS のネイティブ資産を含めない。

公開する `reportdiff-0.1.3-win-x64.zip` は対象コミットから発行し、README・画像・入力サンプル・生成済みレポート・文書・設定例・本体と第三者ライセンスを同梱する。ZIP CRC と全ファイルの SHA-256、コミット済みファイルとの一致を検証し、ZIP と SHA256SUMS を公開する。ログと配布前後の照合記録は `out/releases/v0.1.3/` に保存する。

## 公開時の条件と確認範囲

公開対象コミットを push し、Windows x64／macOS Apple Silicon の CI が成功してから `v0.1.3` タグと GitHub Release を公開する。対象コミット・CI 実行 URL・両 OS の結果・公開アセットの再ダウンロード照合結果はリリースページに記載する。

Windows の完成版 exe をユーザー環境で実行する確認、Windows の Edge／Chrome、低メモリ実機、実帳票の受け入れは未完了。100 ページの合成データ測定は [PERF-2n](perf-2n-continuous.md) の条件に限る。exe は未署名、Intel Mac は対象外。T2-8 の保留候補と T3-1b 以降の行整列は今回のリリース完了に含めない。
