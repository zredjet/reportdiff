# T3-6a 共通色の設定化の確認

2026-09-22、macOS Apple Silicon。[表示式と設定](../planning/t3-6a-overlay-common-color.md)に従い、既定色を `#CCCCCC`、設定キーを `report.raw_overlay_common_color` とした。

- Release ビルドは警告・エラー 0。追加 29 件を含む全 1,368 テスト成功、失敗・スキップ 0。固定画素、全 65,536 グレー値対、非連続 Mat、白・片側・濃淡・A/B 交換、任意 RGB 色、不正設定、継承・上書き、旧 raw_evidence、PDF・一括・no-html を確認した。
- v0.1.3 `f77ac1c` と、42 ゴールデン × 全体補正の有無 × 黒／既定色の 168 条件で比較。表示用の追加設定・メタデータ・実行日時だけを除外し、既存 JSON と終了コードが一致した。黒指定の 84 条件では全 504 PNG がバイト一致。既定色では確認用 PNG 以外の 420 PNG が一致した。
- 両色の確認用 PNG は、保存した元 A/B から NumPy で別計算した全画素と一致した。[互換性集計](t3-6a-color-compatibility.json)。全結果と入力設定は `out/releases/v0.1.4/color-compatibility/`。
- 自作帳票で共通の文字・罫線が薄くなり、赤青の片側成分が見やすくなることを PNG 単体と実画面で確認した。README の画面と同梱 HTML / JSON / PNG を再生成した。
- Chrome 153.0.8010.53 の 1280 / 390px、JavaScript 有効／無効、キーボード、元 A/B との切り替え、単体 PNG、ページと要素の横幅を確認。色見本と色コードを併記し、凡例文字は通常の文字色を維持する。ページエラー・外部通信 0。[ブラウザ記録](t3-6a-color-browser.json)。
- 合成 A4 3 ページ・300dpi・同一入力・確認用画像ありで、CLI 起動から全保存までを測定。初回を除く交互 3 回の中央値は旧版 597ms／変更後 639ms、最大 RSS は 320.7MB／320.3MB、出力は 373,541／375,935 bytes。計測時の変更後バイナリは表示バージョン更新前。一般帳票の速度を保証する測定ではない。[詳細](t3-6a-color-timing.json)。

比較コア、判定用しきい値、元カラー A/B、判定用の重ね描きと切り出しは変更していない。Windows 配布物と両 OS CI は v0.1.4 の公開対象コミットで別途確認し、Windows のユーザー端末・ブラウザ実機・実帳票は未確認として維持する。
