# T3-7 ページ進捗表示の確認

2026-09-22、macOS Apple Silicon。承認された [表示仕様](../planning/t3-7-console-progress.md) を実装した。

- Release ビルドは警告・エラー 0。`dotnet test -c Release` は追加 9 件を含む全 1,339 件成功、失敗・スキップ 0。
- 実プロセスで画像・選択した PDF ページ・片側ページ・一括比較・途中の PDF 読み込みエラー・quiet を確認した。個別失敗後も次のファイルへ進み、既存の終了コードと旧結果保持を維持する。
- PTY に 100 桁／18 桁を設定して合成 3 ページ PDF を実行。100 桁では同一行を 4 回表示し、18 桁では通常の改行表示となった。いずれも終了前に進捗を受信し、最終ページ・レポート出力・完了要約を区別できた。
- 表示単体テストで前の文字の消去、幅縮小後の改行への復帰、任意のファイル名の制御文字除去、quiet 時の端末情報未参照を確認した。
- v0.1.3 の `f77ac1c` を別ディレクトリへ展開してビルドし、42 ゴールデンを全体補正無効／有効の 84 条件で比較する既存の互換性ツールを実行。日時だけを正規化し、終了コードと JSON・HTML・PNG を照合した。

端末出力と互換性の詳細ログは `out/releases/v0.1.4/progress/`・`progress-compatibility/` に保存する。Windows publish と配布 ZIP・両 OS CI は 2 件を含む v0.1.4 のリリース検証で記録する。ユーザーの Windows 端末・実帳票での確認は今回の macOS／PTY 検証に含めない。
