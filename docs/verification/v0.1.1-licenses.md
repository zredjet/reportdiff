# v0.1.1 ライセンス・配布準備の確認

確認日：2026-09-21。対象は本体の MIT License、第三者通知、配布 ZIP、表示バージョン。比較アルゴリズム・設定の既定値・依存バージョン・ネイティブ DLL は変更していない。

## 適用範囲と通知

- ルートの `LICENSE` は MIT License、Copyright (c) 2026 zredjet。
- README・SPEC・配布手順・THIRD_PARTY_NOTICES に、本体の MIT License が第三者の条件を置き換えないことを明記。Intel IPP の EULA と第三者通知は保持する。
- PDFium 内の FreeType は FTL を使用し、FreeType Team の使用謝辞を明記する。
- SkiaSharp の既存の包括通知は改変せず保持し、VulkanMemoryAllocator・D3D12MemoryAllocator の MIT 原文、Wuffs・SPIRV-Cross の Apache-2.0 原文を追加。NuGet の固定コミットから辿った Skia の DEPS に一致する版を使用する。
- 追加原文の取得 URL と SHA-256 は `licenses/sources.json`、採用元とビルド条件は `THIRD_PARTY_NOTICES.md` に記録する。
- 原文の取得・梱包時検査ではハッシュ一致を確認する。これはライセンス適合性全体の法的保証を意味しない。

## ローカル検証

- .NET SDK 10.0.401、macOS Apple Silicon。
- `dotnet build`：警告 0、エラー 0。
- `dotnet test --no-build`：772 件成功、失敗 0、スキップ 0。既存 37 ゴールデンを含む。
- CLI の `--version`：`reportdiff 0.1.1`。JSON 出力の版も 0.1.1 を検証。
- `dotnet publish src/ReportDiff.Cli -p:PublishProfile=win-x64`：成功。exe は 156,547,141 bytes。埋め込み deps.json の自作 4 アセンブリはすべて 0.1.1。
- 必須ネイティブ DLL 3 件の x64・SHA-256、依存一覧、自己完結 .NET 10.0.12 は既存の `licenses/dependencies.json` と一致。FFmpeg・他 OS 資産を含まない。
- ZIP の `LICENSE`・第三者原文・FreeType 謝辞を確認。全ファイルのサイズ・SHA-256 と CRC が一致し、実帳票・作業出力を含まない。
- 別の一時ルートに許諾文だけを置き、`LICENSE` がない条件で実際の梱包処理を呼び出した。`FileNotFoundError` となり、ZIP を残さないことを確認。リポジトリの LICENSE は削除していない。
- 更新文書のローカルリンクと全ライセンス出典ハッシュを確認。

## 公開時の確認と残件

公開対象コミットの Windows x64 / macOS Apple Silicon CI が成功した後にタグを作成し、[v0.1.1](https://github.com/zredjet/reportdiff/releases/tag/v0.1.1) の配布 ZIP と SHA256SUMS を公開する。CI の実行 URL・最終結果はリリースページに記録する。公開アセットは再ダウンロードして手元の ZIP と照合する。

T2-7 の実帳票評価はスキップを維持する。完成版 exe のユーザー実機での実行、Windows 固有の権限・リンク・ジャンクション、Edge / Chrome 表示、署名は今回の検証に含めない。
