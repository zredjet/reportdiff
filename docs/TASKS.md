# 作業リスト

上から順に進める。各タスクは受け入れ条件をすべて満たしたらチェックを付け、1 タスク 1 コミットにする。
仕様は `docs/SPEC.md`、ルールは `CLAUDE.md`。

## 人が行う準備（Claude Code の作業ではない）

- macOS に .NET 10 SDK を入れる。参照実装を動かすなら Python 3 と `pip install opencv-python-headless numpy pillow`
- このキットを空のディレクトリに展開し、`git init` する
- 実帳票のサンプルは `samples/private/` に置く（コミットしない）
- 確認用の Windows（x64）実機を用意する

---

## Phase 0：土台

### [x] T0-1 ソリューションの雛形

- `ReportDiff.sln` と 5 プロジェクト（CLAUDE.md の構成どおり）。参照の向きは Cli → Report → Pdf → Core、Tests → 全部
- `Directory.Build.props`：`net10.0`、`Nullable` 有効、`TreatWarningsAsErrors`、`LangVersion` latest、`InvariantGlobalization` は false
- `.gitignore`（`bin/`、`obj/`、`out/`、`samples/private/`、`__pycache__/`）、`.gitattributes`（`* text=auto eol=lf`、`*.png binary`、`*.pdf binary`）、`.editorconfig`、`THIRD_PARTY_NOTICES.md`（空の表）

受け入れ条件：`dotnet build` が警告ゼロ。`dotnet test` がテスト 0 件で成功。

確認済み（2026-09-20、macOS arm64、SDK 10.0.401）：`dotnet build` は警告 0・エラー 0、`dotnet test` は終了コード 0。テストケースとテストランナーの導入は T0-2 で行うため、この時点のテストは 0 件。サンドボックス内では `--disable-build-servers -m:1` を付けた実行でも成功。

### [x] T0-2 依存の疎通確認（最大のリスクなので最初に潰す）

- NuGet で実在と最新の安定版を確認してから追加：PDFtoImage、OpenCvSharp4、その Windows 用ランタイム、macOS 用ランタイム（Apple Silicon。Intel Mac はユーザー指定により対象外）。YamlDotNet、xUnit
- テスト 1：SkiaSharp の `SKDocument.CreatePdf` で、白地に黒い矩形と赤い線だけの 1 ページ PDF をメモリ上に作る → PDFtoImage で 300dpi にラスタライズ → `Mat`（BGR 8bit）に変換 → 大きさと、矩形の中心・余白の画素の色を検証
- テスト 2：OpenCvSharp で float32 の Lab 変換、`Blur`、`Erode` / `Dilate`、`ConnectedComponents`、`WarpAffine` が動くことを小さな配列で検証
- `dotnet publish -r win-x64`（CLAUDE.md のコマンド）が macOS 上で成功することを確認

受け入れ条件：macOS で 2 つのテストが成功。publish が成功し、出力サイズと含まれるネイティブライブラリ（pdfium、OpenCvSharpExtern、libSkiaSharp の Windows 版）を報告する。
止まる条件：macOS 用の OpenCvSharp ランタイムが解決できない、または publish に Windows 用ネイティブが入らない場合は、作業を止めて状況と選択肢を報告する。

**T0-2 が終わったら、結果を報告して一度止まる。**

確認済み（2026-09-20）：承認された FFmpeg なしの構成を用意した。Windows は公式 slim、macOS は arm64 / x64 のローカルランタイムを生成。macOS arm64 でビルド警告 0・疎通テスト 2 / 2 成功。Windows publish が成功し、exe は 150,507,849 bytes（143.54 MiB）、内部に Windows x64 の pdfium / OpenCvSharpExtern / libSkiaSharp を確認した。Windows での動作 OK をユーザーが確認済み。Intel Mac はユーザー指定により対象外とし、実機確認を残件に含めない。詳細は [T0-2 確認結果](verification/t0-2-dependencies.md)、再生成方法は [ネイティブ依存の準備](NATIVE_RUNTIME.md)。Phase 1 の現在の確認結果は後述。

### [x] T0-3 CI（任意）

GitHub Actions で `windows-latest` と `macos-latest` の両方で `dotnet build` と `dotnet test`。リポジトリが GitHub にない場合は省略。

確認済み（2026-09-21）：Public リポジトリ `zredjet/reportdiff` の `.github/workflows/ci.yml` を push し、[CI #35518444710](https://github.com/zredjet/reportdiff/actions/runs/35518444710) が成功。Windows x64 / macOS Apple Silicon の両方で `dotnet build` は警告 0・エラー 0、`dotnet test` は 2 / 2 成功。macOS の FFmpeg なしランタイムのソースビルドも成功した。詳細は [T0-3 確認結果](verification/t0-3-ci.md)。Phase 1 の現在の確認結果は後述。

CI は `master` への push・pull request と手動実行が対象。ローカルでも actionlint 1.7.12 の検証、macOS arm64 のビルド（警告 0・エラー 0）とテスト（2 / 2 成功）を確認済み。

---

## Phase 1：MVP

### [x] T1-1 単位と設定（SPEC 7 章）

- `Units`（mm↔px）、設定のレコード型、既定値、YAML の読み込み、未知のキーはエラー、値の範囲の検証、プロファイル（normal / strict / loose）
- 優先順位：既定値 < 設定ファイル < `--profile` < `--dpi`

受け入れ条件：既定値が SPEC 7.1 と一致するテスト。未知のキー・範囲外の値が日本語のメッセージ付きでエラーになるテスト。プロファイルと優先順位のテスト。

確認済み（2026-09-21）：設定のレコード型、単位換算、YAML の厳密なキー検査、値検証、プロファイルを実装。明示されたプロファイルだけを設定ファイルより優先し、未指定の image_dpi は最終的な dpi に追従する。macOS arm64 でビルド警告 0、設定 25 件と既存疎通 2 件の計 27 件成功。

### [x] T1-2 比較コア：候補の抽出と位置ずれの吸収（SPEC 5.1〜5.3）

- `reference/prototype.py` の `tolerant_diff` を素直に移植する。この時点では最適化しない（全ページで全ずれを計算してよい）
- 出力：生の差分マスク、`absorbed_groups`、`max_shift_px`

受け入れ条件：T1-4 の一致テストのうち、`raw_pixels`・`absorbed_groups`・`status` の一致。

確認済み（2026-09-21）：全ページ・全ずれの参照方式を移植。37 入力で吸収数と最大ずれが一致し、除外処理前の I05 を除く 36 件で生差分数（±2%）と状態が一致。I05 は除外前に差分が残ることを検証し、除外後の一致も T1-3 で確認済み。ビルド警告 0、計 65 件成功。

### [x] T1-3 比較コア：除外領域とクラスタ化（SPEC 5.4〜5.5）

受け入れ条件：T1-4 の一致テストのクラスタ数と矩形の一致。`too_different`、`CLUSTER_LIMIT`、`noise_dropped` の単体テスト。

確認済み（2026-09-21）：I05 と limit ケースを含む全 37 件で状態・クラスタ数・矩形（±1px）・生差分数（±2%）・吸収数・最大ずれ・ノイズ除去数が一致。除外の端数丸め・範囲クリップ、差分率の境界、ノイズ、クラスタ上限と採用領域の輪郭マスクを検証。ビルド警告 0、計 105 件成功。

### [x] T1-4 ゴールデンテストと最適化（SPEC 9 章）

1. 一致テスト：`reference/golden/expected.json` の全ケース（37 件）を読み、各ケースの `params` で実行。`status` とクラスタ数が一致、矩形 ±1px、`raw_pixels` ±2%。`expect` が `limit` のケースも結果が一致すること
2. 生成テスト：参照実装の `Scene` を C# に移植し、I 系・D 系・S 系を生成して期待（無視／検出、クラスタ数、位置）を検証。J 系は PNG を使う
3. 最適化：SPEC 5.3 の「実装上の注意」のとおり、グループの外接矩形に切り出して評価する。A4・300dpi の合成ページ（文字 600 個、1 か所変更、全体 1px ずれ）で時間を測る簡単な計測手段を用意する

受け入れ条件：1 と 2 が全件成功。最適化の前後で 1 の結果が変わらない。計測結果（完全一致、1 か所変更、1 か所変更＋全体 1px ずれ）を報告する。目標は 2 秒以内、届かない場合は内訳を報告する。

確認済み（2026-09-21）：37 ゴールデンケースが成功し、最適化前後のマスクまで完全一致。C# 生成 31 ケースの入力画像も参照 PNG と完全一致し、検出条件を満たす。計 176 件のテスト成功。A4・300dpi・600 文字の Release 計測（3 回の中央値）は完全一致 5.2ms、1 か所変更 225.8ms、変更＋全体 1px ずれ 245.4ms。詳細・計測内訳・再実行方法は [T1-4 確認結果](verification/t1-4-core.md)。画像読み込みの確認結果は T1-5 に記載。

### [x] T1-5 画像の読み込み（SPEC 4.1、4.3）

受け入れ条件：PNG・JPEG・BMP・TIFF、アルファ付き、グレースケールを BGR 8bit にするテスト。先頭バイトでの形式判定のテスト。日本語と空白を含むパスで読めるテスト。

確認済み（2026-09-21）：4 形式を .NET のファイル読み込みとバイト列のデコードで BGR 8bit 化。白背景への透過合成、8/16bit、TIFF の先頭ページと透明度、シグネチャ判定、日本語・空白パスを検証。画像入力 58 件を追加し、ビルド警告 0・全 234 件成功。詳細は [T1-5 確認結果](verification/t1-5-images.md)。PDF 読み込みの確認結果は T1-6 に記載。

### [x] T1-6 PDF の読み込み（SPEC 4.2）

受け入れ条件：SkiaSharp で作った 3 ページの PDF から、指定ページを指定 DPI で取り出せる。ページの大きさ（px）が「pt / 72 × dpi」と ±1px で一致。16000px を超えるとエラー。日本語パスで読める。

確認済み（2026-09-21）：.NET のストリーム経由で指定ページを白背景の BGR 8bit に描画。3 ページ × 4 DPI の寸法誤差 ±1px、16000px の上限と境界、日本語・空白パス、注釈・フォーム・アンチエイリアスを検証。PDF 入力 36 件を追加し、ビルド警告 0・全 270 件成功。詳細は [T1-6 確認結果](verification/t1-6-pdf.md)。正規化とページ対応の確認結果は T1-7 に記載。

### [x] T1-7 正規化とページの対応（SPEC 4.4、4.5、`--pages`）

受け入れ条件：大きさの違う 2 枚が余白埋めで同サイズになり `SIZE_MISMATCH`。ページ数が違うと `only_in_a` / `only_in_b` と `PAGE_COUNT_MISMATCH`。`--pages "1-2,5"` の解釈と範囲外のエラー。

確認済み（2026-09-21）：拡大縮小せず左上を合わせて右・下を白埋めし、元のサイズ・`SizeMismatch`・警告を保持。ページ順の対応と片側だけのページ、`--pages` の範囲・重複・範囲外エラーを実装。画像・PDF の読み込みから正規化・比較までを含む 55 件を追加し、ビルド警告 0・全 325 件成功。詳細は [T1-7 確認結果](verification/t1-7-pages.md)。結果出力の確認結果は T1-8 に記載。

### [x] T1-8 出力：result.json、重ね描き、切り出し（SPEC 8.1、8.2）

受け入れ条件：スキーマどおりの JSON（テストで逆シリアライズして検証）。相対パスが `/` 区切り。差分のないページの画像は既定で保存されず、`--save-all-pages` で保存される。重ね描きに除外領域（黄色）とクラスタ番号が描かれる。枠の内側のクラスタにも輪郭が描かれる（`RETR_LIST`）。

確認済み（2026-09-21）：実効設定・吸収・ノイズ・サイズ差・警告を含む JSON と、ページ画像・重ね描き・切り出しを実装。JSON の項目名と逆シリアライズ、同一ページの保存切り替え、相対パス、除外領域・番号・入れ子の輪郭、切り出しの画素と余白を検証。出力 15 件を追加し、ビルド警告 0・全 340 件成功。D11 の出力を目視でも確認。詳細と出力例は [T1-8 確認結果](verification/t1-8-output.md)。HTML の確認結果は T1-9 に記載。

### [x] T1-9 HTML レポート（SPEC 8.3）

受け入れ条件：生成した HTML に `http://` と `https://` が含まれない（テストで検査）。A / B / 重ね描きの切り替えが動く。クラスタ一覧に番号、位置（mm）、画素数、3 つの切り出しが並ぶ。警告と吸収数が表示される。`<script type="application/json" id="result">` がある。

確認済み（2026-09-21）：単一 HTML と相対 PNG によるレポートを実装。要約・警告・折りたたみ設定と除外領域・ページ別の吸収数とノイズ数・白埋め・クラスタ一覧・JSON 埋め込みを検証。HTML 19 件を追加し、ビルド警告 0・全 359 件成功。macOS Chrome の `file://` で画像切り替え、キーボード操作、1280 / 390px 幅の表示、外部リクエスト 0 件を確認。詳細は [T1-9 確認結果](verification/t1-9-html.md)。Windows の Edge / Chrome は下記の実機確認に残す。CLI 接続の確認結果は T1-10 に記載。

### [x] T1-10 CLI（SPEC 6 章）

受け入れ条件：終了コード 0 / 1 / 2 のテスト。`--out` が空でないとエラー、`--force` で上書き。エラーは日本語で標準エラー出力。`--quiet`、`--no-html`、`--version`。`Console.OutputEncoding` が UTF-8。

確認済み（2026-09-21）：仕様の CLI オプションを入力・比較・全出力に接続。実プロセスの終了コード 0/1/2 と UTF-8、日本語エラー、設定優先順位、ページ選択、force での置き換えと失敗時の旧結果保持を検証。追加 58 件、ビルド警告 0・全 417 件成功。D11 を CLI から比較して相違 2 箇所・終了コード 1、生成 HTML の Chrome 表示も確認。PDF と画像の DPI 不一致をエラーとする条件はユーザー確認のうえ SPEC に追記。詳細は [T1-10 確認結果](verification/t1-10-cli.md)。指定の PDF 結合シナリオの確認結果は T1-11 に記載。

### [x] T1-11 結合テスト

SkiaSharp で 2 ページの PDF を新旧 2 つ作る（1 ページ目は同一、2 ページ目は 1 か所変更＋除外領域の中だけ別の変更）。CLI を実行する。

受け入れ条件：終了コード 1。`summary.clusters` が 1。1 ページ目は `same`。出力ファイルがそろっている。同一の PDF 同士では終了コード 0。

確認済み（2026-09-21）：SkiaSharp で新旧 2 ページ PDF を生成し、既定の 300dpi・通常設定で CLI 実プロセスを検証。A/B を入れ替えても除外後の変更は 1 箇所・終了コード 1、1 ページ目は same、JSON・HTML・PNG 6 枚がそろう。同一 PDF 同士は終了コード 0。除外なし・除外ページ違いでは 2 箇所になることも確認。結合 6 件を追加し、ビルド警告 0・全 423 件成功。生成 HTML は macOS Chrome の 1280 / 390px 幅で表示・操作・外部リクエスト 0 件を確認。詳細は [T1-11 確認結果](verification/t1-11-integration.md)。配布物の確認結果は T1-12 に記載。

### [x] T1-12 配布物

- `README.md`（使い方、設定例、プロファイルの選び方、既知の限界へのリンク）
- `THIRD_PARTY_NOTICES.md` を完成させる
- win-x64 の単一 exe を作る手順を確認

受け入れ条件：publish が成功。下の「Windows 確認リスト」を最新にする。

確認済み（2026-09-21）：README の使い方・設定例・プロファイル・既知の限界、全文ライセンス通知、Windows 用発行プロファイルと ZIP 作成手順を整備。macOS 上の win-x64 単一 exe の publish が成功し、150,653,769 bytes、必須 DLL 3 種の x64・ハッシュ一致、FFmpeg と他 OS 資産の不在を確認。配布 ZIP の内容・ハッシュと、依存や通知が確認済みの内容から変わった場合の拒否を検証。ビルド警告 0・全 423 件成功。詳細は [T1-12 確認結果](verification/t1-12-distribution.md)。完成版の Windows CI・実機確認は下記に残す。T1-12 時点では Phase 2 は未着手。

---

## Windows 確認リスト（人が実機で行う）

- [x] T0-2 の Windows 動作確認（2026-09-20、ユーザー報告「Windowsは確認。動作OK。」）

T2-6 までを含む v0.1.0 の [CI](https://github.com/zredjet/reportdiff/actions/runs/35558470655) は Windows x64 766 件成功・6 件スキップ、macOS Apple Silicon 772 件成功。従来の「Windows 未実行」は各タスク完了時点の記録であり、CI はその後確認済み。以下の未チェック項目はユーザー実機での個別条件として、CI と分けて確認状況を管理する。

- [ ] T2-6 の配布 ZIP を展開し、.NET を入れていない PC で exe が単体で動く
- [ ] 単一 exe から pdfium / OpenCvSharpExtern / libSkiaSharp が展開・ロードされる（T2-6 もバンドル内の x64 と SHA-256 の静的確認まで）
- [ ] ネイティブ展開先（既定の `%TEMP%/.net` または `DOTNET_BUNDLE_EXTRACT_BASE_DIR`）の権限・空き容量が実行環境で適切である
- [ ] 確認用の Windows 実機上で T0-2 の疎通テスト 2 件が成功する
- [ ] T1-1〜T1-11 の追加テストを Windows でも確認する（macOS では 423 件成功。リンク 5 件・macOS 固有の Unicode 正規化 1 件は Windows ではスキップし、リンクは下記で実機確認。T1-11 の 2 ページ PDF 結合 6 件も Windows では未実行）
- [ ] 日本語・空白を含むパス、長いパスで動く（画像・PDF 入力、JSON・PNG・HTML 出力、T1-10・T1-11 の CLI 実プロセスを含む日本語・空白パスは macOS で確認済み）
- [ ] 完成版 exe を cmd と PowerShell で実行して日本語のメッセージが化けず、終了コード 0/1/2、`--quiet`、`--no-html`、`--version` が動く。同梱の `examples/settings.yaml` が使える
- [ ] `--force` の置き換え・失敗時の旧結果保持、入力・設定ファイルの保護、シンボリックリンク・ジャンクションの拒否とリンク先保護
- [ ] フォント埋め込み済みの同じ PDF で、macOS と同じ `result.json`（クラスタ数と位置）になる
- [ ] フォントが埋め込まれていない帳票での見た目と結果
- [ ] 実帳票（A4・300dpi）での処理時間
- [ ] SmartScreen とウイルス対策ソフトの反応
- [ ] Windows Server で動かす場合：Media Foundation 機能の有無
- [ ] T2-1 の追加 27 テストと合成 PDF の分類（追加・削除・色変更・変更）、切り出しの緑／赤、HTML の分類表示を Windows で確認する
- [ ] T2-2 の追加 54 テストと合成 PDF の移動量・関連 ID・無効化設定、HTML の関連リンク・方向表示を Windows で確認する
- [ ] T2-3 の追加 34 テスト、PDF テキスト注釈・省略と失敗の警告・長文のキーボード操作、PdfPig を含む exe を Windows で確認する
- [ ] T2-4 の追加 51 テスト、YAML による全体補正、補正後の PDF テキスト・除外座標、補正で相違なしになったページの元 B 保存とキーボードによる画像切り替えを Windows で確認する
- [ ] T2-4a の追加 51 テスト、14 項目の YAML 調整・キー名付きエラー・全実効設定の HTML 表示、同梱した5種類の設定例を Windows で確認する
- [ ] T2-5 の追加 60 テスト、使用フォントの非埋め込み・検査未完了の理由と A/B・ページ・名前の JSON / HTML 表示、同一 PDF の終了コード 0、日本語・空白パスを Windows で確認する
- [ ] T2-6 の追加 72 テストと compare-dir の日本語・空白・隠しフォルダ・NFC/大小文字・衝突、YAML の部分上書き・同名アンカー・同梱 rules.yaml、片側未検査・個別エラー継続・終了コード 0/1/2、--force の個別エラー時の置換と全体エラー時の保持、権限・リンク・ジャンクション・再解析ポイントを Windows で確認する
- [ ] T2-6 の index.html を Edge / Chrome の file:// で開き、一覧と個別結果の相対リンク・390px・長い名前・未比較とエラーの表示・キーボード・オフライン動作を確認する
- [ ] `report.html` が Edge / Chrome で `file://` から開け、A / B / 重ね描きを切り替えられる（macOS Chrome では確認済み）

---

## Phase 2（着手時に詳細化。SPEC 11 章）

### [x] T2-1 追加／削除／変更の判定と、切り出し diff の塗り分け（SPEC 11.1）

受け入れ条件：既存の検出・クラスタ所属を維持して A→B のインクを分類する。混在・背景・形状が不明確な場合は changed。除外・ノイズ・上限・入れ子・画像端・通常／厳密・300/400dpi・非連続 Mat を確認する。JSON の kind、緑／赤の切り出し、HTML の日本語ラベルと凡例、合成 PDF の実プロセス結合、オフラインのブラウザ表示、A4 計測を確認する。

確認済み（2026-09-21）：ビルド警告 0・全 450 件成功。37 ゴールデンと最適化前後、分類あり／なしの検出マスク・統計が一致。PDF 結合は既存 6 件＋分類 2 件。macOS Chrome の 1280/390px 表示・操作を確認。A4・300dpi の分類追加処理は約 0.8〜1.0ms、保持する削除マスクは約 8.3MiB。詳細は [T2-1 確認結果](verification/t2-1-classification.md)。T2-1 時点では後続タスクは未着手。

### [x] T2-2 移動の判定（`matchTemplate`、SPEC 11.2）

受け入れ条件：承認済みの各軸 5mm／一致度 0.98／次点との差 0.02 を既定とし、双方向の探索と色・形・元位置の残存差分を確認する。既存の検出結果を維持し、moved / shift_px / related_cluster_ids を JSON と HTML に記録する。方向・境界・反転・通常／厳密・300/400dpi・非連続 Mat・複数クラスタ・除外等・コピーや曖昧な対応を検証する。合成 PDF の実プロセス結合、ブラウザ、A4 と多数候補の時間・メモリを確認する。

確認済み（2026-09-21）：ビルド警告 0・全 504 件成功。既存 37 ゴールデンと最適化前後、移動注釈あり／なしの検出マスク・削除マスク・統計を維持。PDF 結合は既存 8 件＋移動 4 件。macOS Chrome の 1280/390px と関連リンク・キーボード操作を確認。合成 A4 の 120 図形・240 クラスタの移動は約 476ms、通常の 1 か所変更に対する移動判定の追加処理は約 0.9〜1.4ms。詳細は [T2-2 確認結果](verification/t2-2-movement.md)。T2-2 時点では後続タスクは未着手。

### [x] T2-3 PDF テキスト層による注釈（PdfPig、SPEC 11.3）

受け入れ条件：承認済みの単語全文の注釈、CropBox / MediaBox と元サイズの座標対応、境界・除外・順序・重複・Unicode 上限を検証する。画像入力・文字なし・回転・抽出失敗・件数上限の扱いを区別し、画像比較を維持する。JSON / HTML、実 CLI、ブラウザと計測、新規依存の原文通知、Windows publish・バンドル・ZIP を確認する。

確認済み（2026-09-21）：ビルド警告 0・全 538 件成功。既存 37 ゴールデンを維持。自作 Type3 フォントを埋め込んだ合成 PDF で日本語・絵文字・座標・失敗時の継続を検証。追加の実 CLI 結合は 8 件。macOS Chrome の 1280/390px、長文とキーボード、外部通信なしを確認。合成 A4 600 単語、1/120/500 クラスタへの片側の注釈は、オープン込み中央値約 19〜21ms。Windows exe 156,440,645 bytes、PdfPig の管理 DLL 7 件と必須ネイティブ DLL を確認し、通知・ZIP の整合も検証。詳細は [T2-3 確認結果](verification/t2-3-text.md)。T2-3 完了時点では後続タスクは未着手。

### [x] T2-4 全体のずれの推定・補正・報告（SPEC 11.4）

受け入れ条件：[承認済み仕様](planning/t2-4-alignment.md)に従い既定無効で B を整数 px 補正する。候補差・改善・複数区画・端の内容を確認し、未適用理由と補正量、元 B の画像を出力する。既存ゴールデン、内容変更保持、全方向・DPI・上限内外・白紙・局所図形・繰り返し・除外・サイズ差・片側ページ、PDF 座標、実 CLI・HTML、A4 計測、Windows 配布物の静的検証を行う。

確認済み（2026-09-21）：ビルド警告 0・全 589 件成功、追加 51 件。既存 37 ゴールデンを維持。数値変更の差分マスクはずらす前の参照結果と一致し、PDF 文字注釈も補正座標に対応。補正で相違なしでも元 B を含む 4 枚の証拠画像を保存。macOS Chrome の 1280/390px、補正前後の切り替えとキーボード、外部通信なしを確認。A4・300dpi・600 文字の比較までの中央値は全体移動のみ 344.6ms、数値変更併存 550.9ms。Windows exe 156,463,173 bytes、既存依存とネイティブ DLL のハッシュ・FFmpeg 不在・ZIP 整合を確認。詳細は [T2-4 確認結果](verification/t2-4-alignment.md)。T2-4 完了時点では次の T2-4a はタスク化・文書化までだった。

### [x] T2-4a 承認済みパラメータの外部設定整備

受け入れ条件：[計画](planning/t2-4a-configuration.md)と[承認済みの追加案](planning/t2-4a-parameter-catalog.md)に従い、14 項目の設定化、全パラメータの台帳、コメント付き設定例、型・範囲・優先順位・互換性、全実効設定の JSON / HTML、Windows ZIP への同梱を確認する。

確認済み（2026-09-21）：ビルド警告 0・全 640 件成功、追加 51 件。既存 37 ゴールデンを維持し、Python 参照実装でも保存済み PNG の全結果が完全一致。14 項目の非既定値を指定した合成 PDF の実 CLI、入力コメントの保持、33 設定値の HTML 表示を確認。macOS Chrome の 1280/390px・キーボード・外部通信なしを確認。Windows exe 156,481,605 bytes、既存依存・ネイティブ DLL のハッシュ・FFmpeg 不在・5 種類の YAML と設定リファレンスを含む ZIP の整合を確認。詳細は [T2-4a 確認結果](verification/t2-4a-configuration.md)。Windows の実行は未確認。T2-4a 完了時点では T2-5 の前で停止した。

### [x] T2-5 フォント非埋め込みの警告（SPEC 11.5）

受け入れ条件：[採用した仕様](planning/t2-5-fonts.md)に従い、使用フォントの元辞書、標準フォント・サブセット・各形式の埋め込み、未使用・同名・混在・判定不能・検査範囲外を区別する。相違なし・上限超過・片側・選択ページと注釈の省略に独立して警告を収集し、比較結果・終了コードを維持する。JSON / HTML、実 CLI・ブラウザ・計測・Windows 配布物の静的検証を確認する。

確認済み（2026-09-21）：ビルド警告 0・全 700 件成功、追加 60 件（フォント検査 46・実 CLI 14）。既存 37 ゴールデンを維持。自作 TrueType / Type3 の埋め込み・文字取得・描画、元辞書の参照と検査未完了の継続を確認。未使用 Form リソースの循環を修正し回帰検証。macOS Chrome の 1280/390px・長い名前・HTML 特殊文字・キーボード・外部通信なしを確認。A4・600 単語の検査はオープン込み中央値 7.329ms、注釈後の追加分は 0.212ms。Windows exe 156,490,309 bytes、既存依存・ネイティブ DLL のハッシュ・FFmpeg 不在・ZIP の整合を確認。詳細は [T2-5 確認結果](verification/t2-5-fonts.md)。Windows 実行は未確認。T2-5 完了時点では T2-6 の前で停止した。

### [x] T2-6 フォルダ一括比較と一覧レポート、帳票別設定の自動選択（SPEC 11.6）

[承認した仕様・受け入れ条件](planning/t2-6-directory.md)に従い、再帰探索・NFC/大小文字の対応付け、コメント付き YAML の部分上書き、個別エラーの継続、一覧 JSON / HTML、全体単位の出力確定を実装した。

確認済み（2026-09-21）：ビルド警告 0、全 772 件成功、追加 72 件、既存 37 ゴールデンを維持。単一比較との JSON / PNG 一致、後半 PDF ページの失敗時の子出力削除、エラー一覧の force 置換、開始前・出力障害時の旧結果保持、同名 YAML アンカーの修正を検証。macOS Chrome 1280/390px の一覧・個別結果・相対リンク・キーボード・外部通信なしを確認。合成 PNG 300 対の中央値 356.461ms、最大 RSS 中央値 83,378,176 bytes。Windows exe 156,547,141 bytes、既存依存・DLL のハッシュ・FFmpeg 不在・6 種の YAML と ZIP の整合を静的検証。詳細は [T2-6 確認結果](verification/t2-6-directory.md)。Windows 実機は未確認。T2-6 完了時点では T2-7 と push の前で停止した。

### [ ] T2-7 実帳票での評価を受けたパラメータとゴールデンケースの見直し（スキップ）

2026-09-21、ユーザーが実帳票は未準備と確認し、スキップを許可したため見送り。開始点は `b0f2777`。[評価手順・受け入れ条件](planning/t2-7-evaluation.md)、[記録用 YAML](evaluation/case-template.yaml)、Git 対象外のローカル保存先を準備済み。実帳票比較・パラメータ変更・ゴールデン追加は未実施で、受け入れ完了には含めない。実帳票 A/B と人が確認した期待値を用意した後、改めて T2-7 の再開を依頼する。

## リリース整備

### [x] R0-1 MIT License と第三者通知・配布処理の整備（v0.1.1）

本体の MIT License、FreeType 使用謝辞、SkiaSharp 内部依存の原文・出典・ハッシュを追加し、Intel IPP 等の第三者条件との境界を明示する。ZIP に本体の `LICENSE` を同梱し、表示バージョンを 0.1.1 にそろえる。受け入れ条件は警告なしのビルド・全テスト成功・Windows publish・ZIP 内の本体と第三者ライセンスの一致・LICENSE 欠落時の作成拒否。CI と公開結果はリリースページに記録する。

確認済み（2026-09-21）：ビルド警告 0・772 件成功、CLI / JSON / 埋め込みアプリ版は 0.1.1。Windows publish と本体・第三者ライセンスを含む ZIP の検証、LICENSE 欠落時の拒否を確認。詳細は [v0.1.1 配布準備の確認](verification/v0.1.1-licenses.md)。

## Phase 3（検討項目）

- [ ] T3-1 行の挿入に対する帯の整列
- [ ] T3-2 確認結果（確認済・仕様変更・要確認・不具合）の保存と再実行時の引き継ぎ
- [ ] T3-3 GUI（Avalonia）
